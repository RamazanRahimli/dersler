let students=[
    {id:1, name: "Ali", surname: "Alisoy", age:20, grade: 95},
    {id:2, name: "Vusala", surname: "Nabiyeva", age:34, grade: 77},
    {id:3, name: "Aliya", surname: "Gurbanzade", age:14, grade: 98},
    {id:4, name: "Sabir", surname: "Nuruyev", age:15, grade: 54},
    {id:5, name: "Mehman", surname: "Heydarov", age:22, grade: 100},
    {id:6, name: "Sevana", surname: "Mammadli", age:41, grade: 35},
    {id:7, name: "Ilham", surname: "Babayev", age:24, grade: 95},
    {id:8, name: "Namiq", surname: "Gulahmedov", age:20, grade: 95},
    {id:9, name: "Aygun", surname: "Kazimova", age:24, grade: 100}
    ]

    // for (let index = 0; index < students.length; index++) {
    //     if (students[index].age > 25 &&  25 <students[index].age) {
    //         console.log(students[index]);
    //     }
    // }

    // for (let index = 0; index < students.length; index++) {
    //     if (students[index].id ==8) {
    //         console.log(students[index]);
    //     }
    // }

    // for (let index = 0; index < students.length; index++) {  
    //     if (students[index].id > 4 && students[index].id <= 8) {
    //         console.log(students[index]);
    //     }      
    // }

    // for (let index = 0; index < students.length; index++) {
    //     if (students[index].grade > 90) {
    //         console.log('Excellent student grade ; ' + ' ' + students[index].name + ' ' + students[index].surname + ' ' + students[index].grade);
    //     }
    // }

    // for (let index = 0; index < students.length; index++) {  
    //     if (students[index].surname ==  "Mammadli") {
    //         console.log(students[index]);
    //     }      
    // }

    // for (let index = 0; index < students.length; index++) {
    //     if (students[index].name) {
    //         console.log('Telebelerin sayi '+  ' ' + students[index].id);
    //     }
    // }

    // for (let index = 0; index < students.length; index++) {
    //     if (students[index].age > 30 && students[index].grade > 60) {
    //         console.log(students[index]);
    //     }
    // }

    // for (let index = 0; index < students.length; index++) {
    //     if (students[index].age > 20 && students[index].grade > 60 ) {
    //         console.log( ' Yasi 20 den yuxari ve bali 60-dan yuxari olan telebeler ' + students[index].name + ' ' + students[index].surname + students[index].grade);
    //     }
    // }

    // for (let index = 0; index < students.length; index++) {
    //     if ( students[index].age < 20 && students[index].grade > 60  ) {
    //         console.log(' Yasi 20 den asagi ve bali 60-dan yuxari olan telebeler ' + students[index].name + ' ' + students[index].surname + students[index].grade);
    //     }
    // }

    // for (let index = 0; index < students.length; index++) {
    //     if (students[index].age >= 20) {
    //         if (students[index].grade >= 60) {
    //         } else {
    //             console.log("20+,60+" , students[index].name);
    //         }
    //     } else {
    //         console.log("20+,60-" , students[index].name);
    //     }
    // }

    